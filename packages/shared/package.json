{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "TruckControl AI — backend va web uchun umumiy TS tiplari (API kontrakt, enum'lar)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "echo 'shared: faqat tiplar, test yo`q' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
