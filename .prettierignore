# Generated by pnpm, in pnpm's own format. Prettier reformats it, and then the
# next `pnpm install`/`pnpm add` writes it back — leaving a multi-thousand-line
# diff on an unrelated commit. The lockfile's tool of record is pnpm.
pnpm-lock.yaml

# Build output and vendored assets
dist/
build/
coverage/
